@model IList<int>

@{
    ViewBag.Title = "Ninject Demo";
}

<h2>Ninject Demo</h2>

<p>

    <table class="table">
        <tr>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item
                </td>
            </tr>
        }

    </table>
