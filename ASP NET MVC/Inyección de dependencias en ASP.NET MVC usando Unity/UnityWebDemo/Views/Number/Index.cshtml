@model IEnumerable<int>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>

    <table class="table">
        <tr>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item
                </td>
            </tr>
        }

    </table>
